# —— 运行所需（FastAPI 后端 + LoRA 训练 + 数据处理）——
# AutoDL 镜像通常已自带 CUDA 版 torch，请勿重装；如确实缺失，取消下行注释并指定 CUDA 版
# torch>=2.1
transformers==4.53.1
pydantic==2.10.6
accelerate==1.2.1
hf_transfer==0.1.7     # 加快首次模型下载
safetensors==0.4.5

# —— FastAPI 后端（server/app.py）——
fastapi>=0.119
uvicorn>=0.52

# —— LoRA 微调 + 4bit/8bit 量化（server/training.py 使用）——
peft>=0.13.2
bitsandbytes>=0.50

# —— 数据处理（server/data_pipeline/readers.py 读 parquet 用，惰性导入）——
pandas>=2.0
