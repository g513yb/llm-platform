# —— Sprint 0 运行所需 ——
# AutoDL 镜像通常已自带 CUDA 版 torch，请勿重装；如确实缺失，取消下行注释并指定 CUDA 版
# torch>=2.1
transformers==4.53.1
gradio==5.12.0
# 修复 Gradio 5.12 与 Pydantic 2.11+ 的 schema 解析崩溃（_json_schema_to_python_type 布尔子schema）
# 本方案钉到 <2.11；也可改用 gradio>=5.23.1
pydantic==2.10.6
accelerate==1.2.1
hf_transfer==0.1.7     # 加快首次模型下载
safetensors==0.4.5

# —— 可选：8bit 量化（config.QUANTIZATION == "8bit" 时才需安装）——
# bitsandbytes==0.44.1

# —— 后续 Sprint（本期不装）——
# peft==0.13.2        # LoRA 微调
# datasets==3.1.0     # 数据处理
# evaluate==0.4.3     # 多维度评测
